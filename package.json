{
  "name": "photolo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "start:prod": "npm run build && cross-env NODE_ENV=production node server/server.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
